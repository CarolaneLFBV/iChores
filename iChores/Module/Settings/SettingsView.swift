import SwiftUI

struct SettingsView: View {
    var body: some View {
        ZStack {
            Color("AppBackgroundColor")
                .ignoresSafeArea()
            
            VStack {
                Text("SettingsView")
            }
        }
    }
}

